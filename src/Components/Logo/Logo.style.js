import styled from 'styled-components';
import {StyledImg} from "../StyledCpmponents/StyledImg"
import {sizes} from "../styles.function"

export const LogoImg = styled(StyledImg) `
/* @media (max-width: ${sizes.tablet}px) {
       
       width:2rem;
       height:2rem
   
   } */
`